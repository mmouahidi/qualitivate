# ============================================
# Railway Environment Variables
# ============================================
# Copy these to your Railway project's Variables tab
# Railway auto-injects DATABASE_URL when you add a PostgreSQL service
# ============================================

# App Environment
NODE_ENV=production
PORT=5000

# Database
# DATABASE_URL is automatically provided by Railway's PostgreSQL plugin
# No need to set DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD

# JWT Secrets (generate secure random strings)
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<generate-a-64-char-random-string>
JWT_REFRESH_SECRET=<generate-a-different-64-char-random-string>
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# SMTP (for email notifications - optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@qualitivate.io

# Frontend URL (Railway sets RAILWAY_PUBLIC_DOMAIN automatically)
# If not set, the app auto-detects from RAILWAY_PUBLIC_DOMAIN
# FRONTEND_URL=https://your-app.railway.app

# Sentry (optional - error tracking)
# SENTRY_DSN=https://your-sentry-dsn
